# Frontend experience monitoring SDK + ingest service (internal MVP)

- **自建 Grafana（Docker）**：见 [`deploy/grafana/README.md`](deploy/grafana/README.md)（`provisioning` + `docker-compose.yml`，与 `apps/ingest-service` 解耦）。
